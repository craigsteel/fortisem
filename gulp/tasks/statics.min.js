const{src:src,dest:dest,parallel:parallel}=require("gulp"),replace=require("gulp-replace"),DIST_PATH="dist";function fontsFn(){return src("fonts*/**/*").pipe(dest("dist"))}staticsFn=parallel(fontsFn),exports.build=staticsFn;